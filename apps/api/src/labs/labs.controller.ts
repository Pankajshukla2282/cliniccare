import { Body, Controller, Get, Param, ParseIntPipe, Post, Query } from '@nestjs/common'; import { ApiBearerAuth,ApiTags } from '@nestjs/swagger'; import { CurrentUser,OrgId,Permissions,RequestUser,RequireIdempotency } from '../common/decorators'; import { CreateLabReportDto } from './dto'; import { LabsService } from './labs.service';
@ApiTags('labs') @ApiBearerAuth() @Controller('labs') export class LabsController{constructor(private readonly service:LabsService){} @Permissions('medical_record.manage') @RequireIdempotency() @Post('reports') create(@OrgId()o:number,@Body()d:CreateLabReportDto,@CurrentUser()u:RequestUser){return this.service.create(o,d,u.sub)} @Permissions('medical_record.read') @Get('reports') list(@OrgId()o:number,@Query('patientId')p?:string){return this.service.list(o,p?Number(p):undefined)} @Permissions('medical_record.read') @Get('reports/:id') get(@OrgId()o:number,@Param('id',ParseIntPipe)i:number){return this.service.get(o,i)}}
