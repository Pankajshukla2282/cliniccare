apiVersion: v1
kind: Pod
metadata:
  name: postgres-client
  annotations:
    iam.gke.io/allowed-registries: "true"
spec:
  restartPolicy: Never
  containers:
  - name: postgres-client
    image: postgres:16
    command: ["pg_sleep", "3600"]
    env:
    - name: PGPASSWORD
      value: "JWbD4wxpHI7tmGMwtnhb6hwt"